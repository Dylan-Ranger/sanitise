# Node.js (Frontend) Specific - /snap-sanitise-fe
/snap-sanitise-fe/node_modules/          # Ignore node_modules
/snap-sanitise-fe/dist/                  # Ignore build output folder
/snap-sanitise-fe/.env                   # Ignore environment files
/snap-sanitise-fe/.eslintcache           # Ignore ESLint cache
/snap-sanitise-fe/.vite/                 # Ignore Vite-specific folders
/snap-sanitise-fe/package-lock.json      # Ignore package lock file 


# Spring Boot (Backend) Specific - /snap-sanitise-be
/snap-sanitise-be/target/                # Maven build output folder
/snap-sanitise-be/.idea/                 # IntelliJ IDEA configuration files
/snap-sanitise-be/.vscode/               # VS Code configuration files
/snap-sanitise-be/.gradle/               # Gradle build output folder (if using Gradle)
/snap-sanitise-be/.mvn/                  # Maven wrapper files
/snap-sanitise-be/.env                   # Ignore environment files (if applicable)

# Common (For both frontend and backend)
.DS_Store                                # macOS system file
Thumbs.db                                # Windows system file
*.log                                     # Ignore log files
*.log.*                                   # Ignore log files with extensions
.idea/                                    # Ignore JetBrains project files
.vscode/                                  # Ignore VS Code settings folder

# System files
ehthumbs.db
Icon?

